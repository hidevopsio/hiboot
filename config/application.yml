# config file for testing
# filename should be application.yml

app:
  project: hidevopsio
  name: hiboot
  profiles:
    include:
    - foo
    - bar
    - baz

logging:
  level: info

