# config file for testing
# filename should be application.yml

app:
  project: hidevopsio
  name: grpc-client
  profiles:
    active: default
    include:
    - web
    - logging
    - grpc

logging:
  level: info

